using UnityEngine;
using UnityEngine.SceneManagement;

public class GameController : MonoBehaviour
{
    /*
    public Player player;  // Reference to the Player script (assigned in the Inspector)

    private void Update()
    {
        // Check if the 'R' key is pressed to reset the player
        if (Input.GetKeyDown(KeyCode.R))
        {
            ResetPlayer();
        }

        // Check if the 'Enter' key is pressed to load a new map
        if (Input.GetKeyDown(KeyCode.Return))
        {
            LoadMap();
        }
    }

    // Reset the player's position and moveCount
    private void ResetPlayer()
    {
        player.transform.position = new Vector3(-8, 4, 0);  // Reset to the start position
        player.ResetMoveCount();  // Reset moveCount in Player script
    }

    // Load the map (for this example, just loads Map01)
    private void LoadMap()
    {
        string mapToLoad = "Map01";  // Example map name
        SceneManager.LoadScene(mapToLoad);  // Load the scene with that name
    }
    */
}
